# G2.7a execution envelope v1 — delta audit

Date: 2026-09-03
Status: **IMMUTABLE HISTORICAL AUDIT EVIDENCE**

Independent read-only delta audit of the first repository-pinned execution
envelope candidate. This record is evidence, not current status and not an
authorization. It is not rewritten to match later state.

## Audit identity

| Field | Value |
|---|---|
| Audit base | `c02ab75aa30c6c68899f3db65a9391882bfa7258` |
| Audit target | `1c8314945c203d935f35ef09b323713d0d62c92f` |
| Auditor | ChatGPT GPT-5.6 Pro |
| Access mode | GitHub-connected independent read-only delta audit |
| Verdict | `PASS_AFTER_MINOR_PATCH` |
| Blockers | 0 |
| Major findings | 0 |
| Minor findings | 3 |
| Architecture direction | `KEEP_MINIMAL_EXECUTION_DESIGN` |
| Model calls | 0 |

Unlike the preceding structural pre-batch audit, this audit read a
repository-pinned target. Its base and target are both exact commits, so its
findings are independently reproducible from repository state.

## Substantive verification

The audit confirmed that envelope v1 successfully resolves:

| Concern | Resolved |
|---|---|
| P29 one-to-`k` accounting | yes — `k = 6`, script frozen, one scorecard row |
| Exact transmitted-prompt extraction and hash | yes |
| Station-frame payload inclusion | yes |
| Scored-unit / call / session partition | yes |
| Positive and negative isolation controls | yes |
| Post-run session uniqueness | yes |
| Human scorer with no model judge | yes |
| Raw-response traceability | yes |
| Unit-level retry semantics | yes |
| REQUIRED omission disposition | yes |
| Predeclared ambiguity handling | yes |
| Input-reproducibility qualification | yes |
| Architecture and production restraint | yes |

The blocker and all six major findings of the structural pre-batch audit are
treated as resolved at the envelope layer. That audit is preserved separately in
`docs/pilot/g2-structural-prebatch-audit.md` and is not reproduced here.

## Minor findings

| ID | Finding |
|---|---|
| m1 | Section 3 says "seven concepts" but enumerates eight. |
| m2 | The retry-accounting sentence is duplicated in section 6. |
| m3 | The original structural-audit model/provider is not repository-verifiable. This is an honestly disclosed provenance limitation rather than a semantic envelope defect. |

m1 and m2 are textual defects with no semantic effect on the accounting
identity. m3 is not a defect in the envelope at all; it is a property of how the
earlier audit was delivered.

## Advancement

| Question | Result |
|---|---|
| Exact minor patch required before human acceptance? | yes |
| Full structural re-audit required? | no — provided only the authorized corrections are made and deterministic validation confirms no other semantic content changed |
| Fixture/runner implementation eligible? | no |
| Official 58-unit execution authorized? | no |
| Architecture escalation justified? | no |

A `PASS_AFTER_MINOR_PATCH` verdict authorizes the named corrections only. It
does not accept the envelope, does not authorize fixtures or a runner, and does
not authorize execution. Human acceptance and merge remain required.
