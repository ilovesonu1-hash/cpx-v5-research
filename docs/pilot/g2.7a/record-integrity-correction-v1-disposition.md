# G2.7a F1-F3 record-integrity correction disposition

PILOT_ONLY / NON_PRODUCTION

Status at creation: PROPOSED / NOT YET ACCEPTED. Acceptance, if later granted,
is recorded separately; this implementation-session evidence is not an
independent audit or an execution authorization.

## Authority and bounded disposition

- Candidate: `710e6b4b4e7030e25cce1b106401b915a301eb20`.
- Immutable audit checkpoint and correction parent:
  `3e56e8a47ed3274f76548fc92c6daf5490d8ef39`.
- Audit owner: `fixture-runner-candidate-v1-correction-delta-audit.md`.
- Main at bootstrap: `1db27d6c91abf5d2bcc87a0e11bdb4dfb46fbee6`.
- Correction branch: `pilot/g2.7a-record-integrity-correction-v1`.
- Human authority: explicit task to correct only F1-F3, independently re-audit,
  and conditionally accept/merge only after final PASS with no required findings.

All three major findings are accepted for this bounded correction. The earlier
audit remains immutable and its REVISION_REQUIRED verdict is not overwritten.

| Finding | Disposition and implementation |
|---|---|
| F1: nullable session identities | Accept. Every raw call and every post-creation terminal event requires a valid non-null public-safe session ID. A genuine creation failure has an explicit null ID and no calls. Existing per-attempt consistency, cross-attempt uniqueness, retry separation and preflight disjointness checks remain in force. |
| F2: authoritative error calls | Accept. A completed authoritative attempt requires every planned call to have a final string (including empty), nonempty completion status other than execution_error, null errors, authority and verified isolation, plus one coherent successful terminal event. Error calls cannot be authoritative. Failed-call events match the final error call; close failure never certifies isolation. |
| F3: malformed failed prefixes and retry after success | Accept. Every attempt is an exact duplicate-free ordered prefix of its unit plan. A call error ends the prefix. Non-complete attempts and their events remain nonauthoritative. The first safely completed attempt is terminal: no later attempt is allowed. Invalid corpus evidence yields no authoritative score-evidence mapping. |

Changes are limited to the deterministic record validator, its adversarial tests,
this disposition and CURRENT_STATE. There is no runner orchestration redesign,
new state machine, transport adapter, semantic scorer or production integration.
The runner's existing three-attempt lifecycle and valid poor/empty-output behavior
are unchanged and covered by regression tests.

## Implementation-session verification

Commands use Python 3.11+ with `-B`; no dependencies were installed. Repository
relative commands (each returned exit 0):

```text
python -B tools/pilot/g2_7a/build_bundle.py
python -B tools/pilot/g2_7a/build_bundle.py
python -B tools/pilot/g2_7a/build_bundle.py --check
python -B tools/pilot/g2_7a/validate_bundle.py
python -B tools/pilot/g2_7a/runner.py plan
python -B tools/pilot/g2_7a/runner.py validate
python -B -m unittest discover -s tools/pilot/g2_7a/tests -v
git diff --check
```

- Bundle validator: 184 deterministic checks, 0 errors.
- Completed synthetic full-corpus validator: 26 checks, 0 errors, 58 ordered
  evidence mappings; this is not official output or semantic scoring.
- Unit tests: 60 passed (40 existing, 20 new test methods with adversarial
  subcases). Tests use only memory/fakes and prohibit network/external commands.
- Builder: both builds reproduced all 19 generated artifacts; --check passed;
  no generated bytes changed from the audit parent.
- Plan: 58 scored units / 64 official calls / 48 sessions; P29 has six calls.
- JSON: all six manifests parsed. CSV: 58 rows with no dispositions prefilled.
- Previous exact F1 full-corpus reproduction (all call/event session IDs null):
  rejected with RUN_SAFE_SESSION_ID_INVALID; no evidence mappings returned.
- Previous exact F2 full-corpus reproduction (one authoritative error call with
  a successful terminal event): rejected with RUN_ATTEMPT_TERMINAL_CALL_CONTRADICTION
  and RUN_AUTHORITATIVE_ATTEMPT_INCOMPLETE; no evidence mappings returned.
- Full-corpus reversed, duplicate, skipped, extra and unknown failed-prefix calls,
  retry after success, missing/mismatched IDs, error/event contradictions and
  close-failure isolation contradictions are rejected in the new tests.
- Valid partial failed prefix followed by a new-session complete attempt passes;
  failed evidence is preserved and excluded from ordered authoritative evidence.

## Unchanged identities and stopping boundary

Input bundle remains
`sha256:991159fc06f2f135f97422995da029442d79584269c2fc2d10cf33011feed317`.
Validator/test implementation identity is a separate accepted checkpoint, never
a self-referential component of this input bundle.

- Semantic checkpoint: `33cca3781b301c29af965430c3caaf32378c28ff`.
- Envelope checkpoint: `77b17f9a5716e67b7ccaf2c589572cc4b0ea23c4`.
- Prompt document SHA-256:
  `373351aaa9d254e42a88c0daf209124fefb3ff59fc59571095345e28ea451d72`.
- Prompt block SHA-256:
  `f4df500f622633480cd9525fa2f61e57c94921af12fa51b0842b506d8f9040b8`.
- Nonce: `CPX-G2-ISO-012fb514aefc248b0e058f8f98c0e43e` (unchanged).

No prompt, contract, trajectory, evaluation specification, envelope, patient fact,
payload, frozen manifest/spec/template, or prior audit bytes changed. No human
review question was adjudicated. No authorization or official raw output exists.
HUMAN remains the semantic scorer; MODEL_JUDGE=NONE.

The next permitted action is an independent read-only final-state/delta audit in
a clean separate context with all offline checks and prior reproductions rerun.
Acceptance/merge is conditional on PASS with 0 blockers, 0 major findings and no
required minor findings, byte-idempotence, unchanged protected inputs and disabled
execution. Audit failure stops advancement. Target HARNESS_CAPABILITY_UNVERIFIED,
real adapter absent, SESSION_ISOLATION_BASELINE=NOT_ESTABLISHED,
ISOLATION_PREFLIGHT_EXECUTED=NO, PATIENT_MODEL_CALLS=0,
G2_FULL_COVERAGE=NOT_EXECUTED/UNAUTHORIZED, MODEL_JUDGE=NONE,
ARCHITECTURE_ESCALATION=NOT_JUSTIFIED and
PRODUCTION_IMPLEMENTATION_AUTHORIZED=NO remain unchanged.
